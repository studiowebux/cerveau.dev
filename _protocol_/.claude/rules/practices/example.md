# Example Practice Rule

Add your practice rules here. One file per practice.

Example filenames: `testing.md`, `code-review.md`, `error-handling.md`

Declare which practices a brain uses in `_configs_/brains.json`:

```json
{
  "practices": ["testing", "code-review"]
}
```

Only declared practices get symlinked into the brain.
